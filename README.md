# Сайт Александра Белякова — техническая документация

Этот файл — справка для владельца сайта (не программиста) и для новой сессии Claude Code, чтобы быстро понять структуру и историю проекта без необходимости пересматривать всю переписку.

## 1. Общее описание

- **Что это:** лендинг частного специалиста по Яндекс Директу (Александр Беляков) + раздел бесплатных обучающих материалов (гайдов).
- **Домен:** `direct.alex-belyakov.com` (задаётся файлом `CNAME` в корне репозитория).
- **Репозиторий:** https://github.com/Aleksandr-Beliakov/direct
- **Хостинг:** GitHub Pages, статический сайт без сборки — чистый HTML/CSS/JS, без npm/фреймворков.
- **Шрифт:** Google Fonts «Golos Text», подключается на каждой странице через `<link>` в `<head>`.
- **Стиль вёрстки:** большая часть оформления — инлайн `style="..."` прямо в HTML (так исторически сложилось при переносе из макета Claude Design). В `css/style.css` вынесены только адаптивные media-запросы и несколько именованных компонентов (FAQ-аккордеон, попапы).

## 2. Структура файлов и папок

```
/index.html               — главная страница (весь лендинг одним файлом, секции идут одна за другой)
/privacy.html              — страница политики конфиденциальности (расширенная версия с разделом про cookie/веб-аналитику, якорь `id="cookies"`, см. раздел 7)
/materials.html             — хаб-страница «Бесплатные материалы» (список из 3 гайдов)
/thanks.html                — страница «спасибо» после отправки любой формы, КРОМЕ формы подписки на гайды
/thanks-materials.html      — страница «спасибо» после формы подписки на гайды (forma-gaidy), с автопереходом через 3 сек
/404.html                   — страница «страница не найдена», в стиле сайта; GitHub Pages сам отдаёт её с кодом 404 для любого несуществующего адреса
/CNAME                      — файл для GitHub Pages, содержит домен direct.alex-belyakov.com

/css/style.css              — общие стили: адаптивность (media-запросы), мобильная шапка-карточка с гамбургер-меню, кружки-иконки мессенджеров с hover-анимацией, hover-эффект оранжевых кнопок, FAQ-аккордеон, попапы, подсказки форм, баннер согласия на cookie
/js/main.js                 — общая логика интерфейса: FAQ-аккордеон, лайтбокс сертификатов, попапы (открыть/закрыть), мобильное меню-гамбургер (открыть/закрыть), закрытие по Esc и клику вне
/js/forms.js                 — отправка всех форм на Web3Forms (см. раздел 4)
/js/cookie-consent.js        — баннер согласия на cookie + отложенная загрузка аналитики (см. раздел 7)

/images/                    — все картинки сайта (фото, сертификаты, иконки, обложки материалов, favicon)
/materials/                 — папка с полными текстами гайдов (лендинги-статьи)
  /materials/25-voprosov-podryadchiku.html
  /materials/kak-proverit-kabinet.html
  /materials/minus-slova-rsya.html
```

### Что лежит в /images/

| Файл | Для чего |
|---|---|
| `my-photo.jpg` | фото в хиро-блоке главной |
| `about-photo.jpg` | фото в блоке «Обо мне» |
| `certificate-direct.png`, `certificate-metrika.png` | круглые бейджи сертификатов в хиро-блоке |
| `direct2026.jpg`, `metrika2026.jpg` | полные сканы сертификатов (блок «Подтверждённая экспертиза», открываются в лайтбоксе по клику) |
| `25questions.png`, `15min.png`, `top_stop.png` | обложки трёх материалов (используются и в блоке «Полезное» на главной, и в карточках `materials.html`) |
| `review_1.png`, `review_2.png`, `review_3.png` | скрины отзывов — **временные заглушки, см. раздел 6** |
| `kabinet-cover.png` | обложка-иллюстрация статьи «Как самому зайти в кабинет и проверить рекламу» |
| `cover-25-voprosov.png` | обложка-иллюстрация статьи «25 вопросов, чтобы проверить подрядчика» |
| `cover-minus-slova-rsya.png` | обложка-иллюстрация статьи «Минус-слова и площадки РСЯ» |
| `screenshot-ploshadki-rashod.png`, `screenshot-opasnoe-slovo.jpg` | реальные скриншоты внутри статьи «Минус-слова и площадки РСЯ» (открываются в лайтбоксе по клику), заменили текстовые плейсхолдеры |
| `face-avatar.jpg` | круглое фото лица — используется в мобильной шапке (рядом с именем) и в финальных CTA-блоках статей материалов |
| `favicon.png` | иконка вкладки браузера, подключена на всех страницах сайта |

## 3. Все формы на сайте

На сайте 6 разных форм. Пять из них ведут на `thanks.html`, одна (подписка на гайды) — на `thanks-materials.html`.

| Название (внутреннее) | Где на сайте | Поля (name → что значит) |
|---|---|---|
| **Форма внизу главной** (`id="form-request"`, секция `#request`) | Финальный тёмный блок в самом низу главной страницы | `name` — имя клиента **(обязательно)**; `messenger` — поле подписано «Контакт для связи», плейсхолдер «Мессенджер или телефон» **(обязательно)**; `link` — ссылка на сайт/рекламный аккаунт (необязательно); `comment` — комментарий (необязательно) |
| **forma-audit** (`id="popup-audit"`) | Попап «Получите бесплатный разбор рекламы». Открывается с кнопок «Получить аудит» (шапка), «Получить бесплатный аудит» (хиро, карточка боли №2, блок бесплатного аудита, кейсы «Хочу также»). Также подключена отдельной копией на всех 3 страницах гайдов в `/materials/` и на `materials.html` (там кнопка «Получить бесплатный аудит» в финальном блоке страницы) | `name` — имя (необязательно); `phone` — телефон, плейсхолдер `+7 (000) 000-00-00` **(обязательно, проверяется формат — см. раздел 4)** |
| **forma-obsudit** (`id="popup-obsudit"`) | Попап «Обсудить проект». Открывается с кнопок «Обсудить проект» (хиро, карточка боли №3) и «Обсудить ведение» (тариф «Месячное ведение») | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)** |
| **forma-zakazat** (`id="popup-zakazat"`) | Попап «Заказать настройку». Открывается с кнопки «Заказать настройку» (тариф «Разовый запуск») | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)** |
| **forma-mediaplan** (`id="popup-mediaplan"`) | Попап «Получить медиаплан». Открывается с кнопки «Получить бесплатный медиаплан» (карточка боли №1) | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)** |
| **forma-gaidy** (`id="popup-gaidy"`) | Попап «Оставьте Email, чтобы открыть материалы». Открывается с трёх кнопок «Скачать бесплатно» в блоке «Бесплатные материалы» на главной. **Другая логика редиректа** — см. раздел 4 | `email` — почта **(обязательно)** |

Во всех попапах-заявках (`forma-audit`, `forma-obsudit`, `forma-zakazat`, `forma-mediaplan`, а также в попапе на `materials.html` и на всех страницах гайдов), кроме `forma-gaidy`, над полем телефона стоит ряд из 3 кнопок-ссылок «Написать в мессенджер» (Telegram / MAX / WhatsApp, ведут на `t.me/a_beliakov`, `max.ru/...`, `wa.me/79890473181`) — это не часть формы, просто быстрый способ написать напрямую, минуя отправку заявки.

Общие для всех форм технические поля:
- `consent` — чекбокс согласия на обработку персональных данных. **Обязателен**: без галочки форма не отправится, рядом появится подсказка.
- Поля, помеченные выше как «обязательно», тоже не дают отправить форму пустыми — браузерная стандартная подсказка отключена (`novalidate`), вместо неё показывается своя подсказка на русском рядом с кнопкой и подсвечивается конкретное незаполненное поле.
- `subject` (скрытое поле) — пишет в письмо, какая именно форма/страница прислала заявку, например «Заявка с сайта — попап «Заказать настройку»». Без этого поля все письма выглядели бы одинаково.
- `botcheck` (скрытое поле-ловушка для ботов, honeypot) — см. раздел 4.

Три текстовых ссылки «Или скачайте: …» под карточками боли на главной — это **не формы**, а обычные якорные ссылки на блок `#materials` (см. раздел 5).

## 4. Как устроена отправка форм

- **Сервис:** [Web3Forms](https://web3forms.com) — сторонний сервис приёма форм без бэкенда, письмо с содержимым формы приходит на почту владельца ключа. Общий для всех форм access key хранится прямо в `js/forms.js` (не в этом файле — ключ намеренно не публикуется в документации).
- **Как работает `js/forms.js`:** один общий скрипт находит на странице все `<form data-redirect="...">`, при отправке:
  1. Проверяет обязательные текстовые поля (`required`, но с отключённой браузерной подсказкой через `novalidate`). Если что-то пустое — форма не отправляется, показывается своя подсказка на русском, и фокус ставится на первое незаполненное поле. Поле `email` дополнительно проверяется на формат (должно быть похоже на настоящий email), поле `phone` — на формат телефона (только цифры/пробелы/`+`/скобки/дефисы, и 10–11 цифр внутри), иначе — своя подсказка «Введите настоящий email/номер телефона, например ...».
  2. Проверяет чекбокс `consent`. Если не отмечен — форма не отправляется, показывается подсказка рядом с галочкой.
  3. Собирает все реально существующие поля формы через `FormData` и отправляет POST-запросом на `https://api.web3forms.com/submit`.
  4. Если Web3Forms ответил успехом — в `window.dataLayer` пушится событие `{ event: 'form_sent', form_id: '<id формы>' }` (для аналитики/целей в Метрике или GTM), и браузер переходит на адрес из `data-redirect` (`thanks.html` или `thanks-materials.html`, для страниц в `/materials/` — с префиксом `../`).
  5. Если сеть недоступна или Web3Forms вернул ошибку — кнопка возвращается в исходное состояние, рядом с формой показывается текст «Не получилось отправить, напишите мне напрямую в Telegram» со ссылкой на https://t.me/a_beliakov.
- **Поле `subject`** — задаёт «тему» письма на стороне Web3Forms, по нему в почте сразу видно, с какой именно кнопки/попапа пришла заявка.
- **Honeypot (`botcheck`)** — скрытый чекбокс, невидимый обычному человеку (`display:none`), но который боты-скрипты иногда всё равно заполняют. Web3Forms распознаёт такие отправки как спам и отсекает их. Реализовано на всех формах сайта.
- **Куда приходит почта:** на адрес, привязанный к access key в личном кабинете Web3Forms (настраивается на их стороне, в коде сайта этого адреса нет).
- **Событие `form_sent`** — если на сайт подключат Google Tag Manager или используют цели Метрики через dataLayer, это событие уже пушится при каждой успешной отправке любой из 10 форм, с полем `form_id`, по которому можно различать, какая именно форма сработала.

## 5. Якоря и ссылки

| Якорь/ссылка | Где ведёт | Комментарий |
|---|---|---|
| `#services` | Секция «Услуги и цены» на главной | пункт меню шапки (на мобильной ширине — внутри выпадающего меню-гамбургера) |
| `#process` | Секция «Как проходит работа» на главной | пункт меню шапки (на мобильной ширине — внутри выпадающего меню-гамбургера) |
| `#cases` | Секция «Результаты в цифрах» на главной | пункт меню шапки (на мобильной ширине — внутри выпадающего меню-гамбургера) |
| `#materials` | Секция «Бесплатные материалы» на главной | со ссылок «Или скачайте: …» под карточками боли |
| `#request` | Секция с финальной формой внизу главной (она же футер) | используется как fallback-адрес у кнопок-триггеров попапов (если JS не сработает — просто проскроллит к форме внизу) |
| `#faq` | Секция «Частые вопросы» на главной | пункт «Вопросы» в навигации футера |
| `#free-audit`, `#audit-example` | Блок бесплатного аудита / блок-пример аудита | кнопка «Посмотреть пример аудита →» |
| `privacy.html` / `../privacy.html` (из `/materials/`) | Страница политики конфиденциальности | ссылка в чекбоксах согласия всех форм |
| `materials.html` | Хаб-страница со списком гайдов | пока никак не связана с главной страницей напрямую, см. раздел 6 |

## 6. Что сделано и что отложено

**Сделано и работает:**
- Вся вёрстка главной страницы, политика конфиденциальности, хаб материалов, 3 полные статьи-гайда и страница 404.
- Единая шапка (логотип, меню, телефон, мессенджеры, кнопка аудита) — одинаковая на главной, `materials.html` и всех 3 гайдах.
- **Мобильная шапка (≤600px)** — компактная плоская полоса (без карточки/тени). Слева круглое фото лица (36–40px) с зелёной точкой-статусом, рядом имя и подпись («Яндекс Директ ·» на первой строке, «частный специалист» — на второй, склеены неразрывным пробелом, чтобы не разрывались). Справа два круглых значка: универсальный контакт-тоггл (открывает выпадающий список «Позвонить» + 3 мессенджера) и гамбургер (открывает меню «Услуги/Процесс/Кейсы»). На **главной странице** под этой строкой на всю ширину шапки идёт растянутая капсула «Сертифицированный специалист «Директ Про»»; сама шапка на этой ширине зафиксирована (`position: fixed`, класс `ab-header-home`) и остаётся видимой при прокрутке — у первого экрана и у первой секции остальных страниц с такой шапкой (`materials.html` и все гайды в `/materials/`) добавлен компенирующий отступ сверху, чтобы контент не прятался под шапкой. Телефон, ряд мессенджеров и кнопка «Получить аудит» из шапки на мобильном убраны — дублируются ниже по странице. Оба меню закрываются по клику вне, по клику на пункт и по Esc, открытие одного закрывает другое.
- **Мобильный первый экран (hero) на главной** — компактный статус-блок вместо развёрнутой карточки: только фраза «Отвечаю лично, в течение 2 часов» и то была убрана (аватар и статус переехали в шапку, см. выше), блок цифр (10 лет / Лично / Договор) поднят сразу под кнопки CTA. Заголовок и подзаголовок при этом показывают тот же текст, что на десктопе (с оранжевым выделением слова «заявки» в заголовке) — сокращённые мобильные варианты текста (классы `*-short`) остались в разметке, но сейчас не используются. Карточка сертификатов на мобильном (`.ab-hero-certs-combined`) оформлена как десктопные карточки: круглая иконка, «Директ Про · Метрика», «Сертифицированный специалист», кнопка «Проверить сертификаты» → `https://yandex.ru/adv/expert/certificates/2e4833R2R`.
- 10 форм (6 уникальных, из них `forma-audit` продублирована на `materials.html` и 3 страницах гайдов) подключены к Web3Forms: обязательные поля и согласие валидируются на русском без системных браузерных подсказок, honeypot от ботов, разные страницы благодарности, событие `form_sent` в dataLayer при успехе.
- Favicon подключён на всех страницах.
- Сертификаты открываются в новой вкладке, ссылки на мессенджеры — тоже.
- Иконки мессенджеров в шапке — брендовые SVG (Telegram/WhatsApp/MAX), одинаковые круглые кнопки 40×40px с hover-анимацией (приподнимаются и красятся в фирменный цвет мессенджера).
- Все оранжевые кнопки на сайте плавно темнеют при наведении (`#FF5A1F` → `#E8480F`).
- Ссылка на Telegram везде на сайте — `https://t.me/a_beliakov`.
- Баннер согласия на cookie (компактная тёмная карточка внизу слева, без кнопки «Отклонить») на всех 9 страницах — Яндекс.Метрика теперь загружается только после согласия пользователя (см. раздел 7).
- `thanks-materials.html` (страница после подписки на гайды) — и автопереход через 3 сек, и кнопка «Перейти к материалам» ведут на `materials.html` (раньше был временный якорь `index.html#materials`).
- Финальная секция `#request` теперь совмещает роль футера: 3 колонки (имя/подпись/«на связи»/мессенджеры/почта → навигация → сама форма заявки, без изменений в её полях), под ними строка с реквизитами («Самозанятый Беляков А.В., ИНН 501811176988. Работаю по договору», ссылка на политику, год). Отдельного `<footer>` на сайте нет — это по-прежнему один `<section id="request">`. Фраза «Работаю по договору» на мобильном обёрнута в `white-space:nowrap`, чтобы не разрывалась при переносе.
- **Попапы-заявки переработаны** (все, кроме `forma-gaidy`): добавлено необязательное поле «Ваше имя», под подзаголовком — ряд из 3 кнопок быстрой связи через мессенджеры (см. раздел 3), разделитель «или оставьте заявку», у поля телефона новый плейсхолдер `+7 (000) 000-00-00` и проверка формата (раздел 4).
- Страница `materials.html` получила такой же финальный тёмный CTA-блок («Не хватает времени разбираться самому?» + кнопка аудита), как у статей-гайдов — раньше его не было.
- Обложки-иллюстрации у всех 3 статей-гайдов теперь реальные картинки (не плейсхолдеры) — `cover-25-voprosov.png`, `cover-minus-slova-rsya.png`, `kabinet-cover.png`. Внутри статьи «Минус-слова и площадки РСЯ» два текстовых плейсхолдера-скриншота тоже заменены на реальные картинки, открывающиеся в лайтбоксе по клику.
- Email на сайте (футер главной, `privacy.html`) — `me@alex-belyakov.com`.

**Осознанно отложено:**
- **Прямой переход с главной страницы на `/materials.html` (хаб гайдов) пока не добавлен.** Раздел существует, но нигде на главной на него нет ссылки — решили подключить позже.
- **Кнопки «Скачать бесплатно» в блоке «Полезное»** на главной технически ведут на несуществующие файлы `/materials/*.pdf` (это просто fallback-адрес на случай, если JS не сработает) — реальная логика скачивания завязана на попап `forma-gaidy` и его дальнейший сценарий на `thanks-materials.html`.
- **Ссылки «Подробнее →» у трёх кейсов** в блоке «Результаты в цифрах» ведут на черновые страницы-заглушки `/cases/case-1.html`, `/cases/case-2.html`, `/cases/case-3.html` («Кейс в процессе оформления, скоро будет опубликован» + кнопка назад). Полноценные страницы кейсов ещё предстоит написать и подключить вместо заглушек.
- **Отзывы (`review_1.png`, `review_2.png`, `review_3.png`)** — три файла в `/images/` **побайтово идентичны** (одна и та же картинка под тремя именами), это временные заглушки-скриншоты. Подпись под ними тоже пока заглушка: «Имя · ниша». Ждут замены на реальные отзывы.

## 7. Индексация поисковиками (временно отключена)

Пока идёт подготовка сайта, индексация в поисковиках **намеренно закрыта**:
- На всех 9 страницах в `<head>` стоит `<meta name="robots" content="noindex, nofollow">`.
- В корне репозитория лежит `robots.txt` с `Disallow: /` для всех роботов.

Сайт при этом работает как обычно — это не блокирует доступ людям по прямым ссылкам, только просит поисковики не добавлять страницы в выдачу. **Когда сайт будет готов к запуску, оба этих ограничения нужно снять** (убрать мета-тег на всех страницах и поправить/удалить `robots.txt`), иначе сайт так и не появится в поиске.

## 8. Внешние сервисы

- **Web3Forms** — приём и пересылка данных всех форм сайта на почту владельца. Никакого бэкенда/базы данных на сайте нет, вся обработка форм происходит на стороне Web3Forms. Access key используется единый для всех форм, хранится в `js/forms.js` (сам ключ в этой документации не публикуется).
- **GitHub Pages** — хостинг статического сайта. Домен `direct.alex-belyakov.com` подключён через файл `CNAME` в корне репозитория + настройку custom domain в настройках репозитория на GitHub (там же выпускается HTTPS-сертификат).
- **Google Fonts** — подключение шрифта Golos Text через `<link>` на каждой странице (CDN Google, не требует настройки).
- **Яндекс.Метрика + баннер согласия на cookie** — счётчик (id `111606163`) больше **не** вставлен напрямую в `<head>`. Вместо этого на каждой странице перед `</body>` подключён `<script src="/js/cookie-consent.js" defer></script>` (путь абсолютный от корня — работает одинаково на страницах любой вложенности, включая `/materials/*`).
  - При первом визите показывается компактная тёмная карточка (не на всю ширину, плавает снизу слева) с текстом, ссылкой на `/privacy.html#cookies` и одной кнопкой «Принять». Кнопки «Отклонить» нет — согласие даётся явным кликом или продолжением пользования сайтом.
  - Согласие сохраняется в `localStorage.cookie_consent = 'accepted'`. Пока пользователь не нажал «Принять», карточка показывается на каждой странице.
  - Код счётчика Метрики (те же id и параметры, что были раньше) теперь живёт внутри функции `loadAnalytics()` в `js/cookie-consent.js` и вызывается только при нажатии «Принять» или если согласие уже было дано ранее. При отказе или до выбора — счётчик не грузится вообще.
  - Google Ads/Analytics на сайте пока не подключены; когда появится код счётчика — его нужно добавлять точно так же, внутрь `loadAnalytics()`, а не отдельной вставкой в `<head>`.
  - **При создании любой новой страницы сайта** счётчик в `<head>` добавлять не нужно — достаточно подключить `js/cookie-consent.js` перед `</body>`, как на остальных 9 страницах.
