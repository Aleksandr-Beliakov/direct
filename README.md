# Сайт Александра Белякова — техническая документация

Этот файл — справка для владельца сайта (не программиста) и для новой сессии Claude Code, чтобы быстро понять структуру и историю проекта без необходимости пересматривать всю переписку.

## 1. Общее описание

- **Что это:** лендинг частного специалиста по Яндекс Директу (Александр Беляков).
- **Домен:** `direct.alex-belyakov.com` (задаётся файлом `CNAME` в корне репозитория).
- **Репозиторий:** https://github.com/Aleksandr-Beliakov/direct
- **Хостинг:** GitHub Pages, статический сайт без сборки — чистый HTML/CSS/JS, без npm/фреймворков.
- **Шрифт:** Google Fonts «Golos Text», подключается на каждой странице через `<link>` в `<head>`.
- **Стиль вёрстки:** большая часть оформления — инлайн `style="..."` прямо в HTML (так исторически сложилось при переносе из макета Claude Design). В `css/style.css` вынесены только адаптивные media-запросы и несколько именованных компонентов (FAQ-аккордеон, попапы, карусели).
- **Три варианта главной страницы** (см. раздел 2 и раздел 6): `index.html` — боевая версия, `new.html` — рабочий черновик для экспериментов (обычно идентичен `index.html`, правки сначала обкатывают тут), `pilot.html` — замороженный архив прежней боевой версии до большого редизайна 2026-08.

## 2. Структура файлов и папок

```
/index.html               — главная страница, боевая версия (весь лендинг одним файлом, секции идут одна за другой)
/new.html                  — рабочий черновик главной для экспериментов. На момент последнего переноса содержимое идентично index.html — сюда сначала вносят и проверяют изменения дизайна, потом переносят на index.html. Скрыт от индексации (noindex) и заблокирован в robots.txt
/pilot.html                 — архивная копия прежней боевой версии сайта (до редизайна). Хранится для истории/отката, никакие страницы сайта на неё не ссылаются. Скрыт от индексации (noindex) и заблокирован в robots.txt
/privacy.html              — страница политики конфиденциальности (расширенная версия с разделом про cookie/веб-аналитику, якорь `id="cookies"`, см. раздел 8)
/thanks.html                — страница «спасибо» после отправки любой формы
/404.html                   — страница «страница не найдена», в стиле сайта; GitHub Pages сам отдаёт её с кодом 404 для любого несуществующего адреса
/CNAME                      — файл для GitHub Pages, содержит домен direct.alex-belyakov.com
/robots.txt                 — инструкция для поисковых роботов, см. раздел 7
/sitemap.xml                — карта сайта для поисковиков, см. раздел 7
/favicon.ico                — иконка вкладки браузера в формате .ico (16/32/48/64px) прямо в корне — часть браузеров и внешних сервисов запрашивают именно `/favicon.ico` по умолчанию, не читая `<link rel="icon">` из `<head>`. Основной favicon подключён отдельно через `<link>` (см. `images/favicon.png` ниже) — этот файл не заменяет его, а закрывает автозапрос по стандартному адресу
/yandex_bdb97dc3a28e4f23.html — файл подтверждения прав на домен для Яндекс.Вебмастера. Не удалять — по нему Яндекс периодически перепроверяет владение доменом

/cases/                     — страницы с кейсами, см. раздел 6.1
  /cases/bani-bochki-mo.html — единственный на сегодня полноценный кейс, «Бани-бочки» (Москва и область). Пока не связан ссылками с `index.html`/`new.html` — открывается только по прямому адресу, см. раздел 6.1

/css/style.css              — общие стили: адаптивность (media-запросы), мобильная шапка-карточка с гамбургер-меню, кружки-иконки мессенджеров с hover-анимацией, hover-эффект оранжевых кнопок, FAQ-аккордеон, попапы, подсказки форм, баннер согласия на cookie, мобильные слайдеры-карусели (отзывы, процесс, сравнение), блоки кейс-страницы и бесконечная галерея креативов (`.ab-case-*`), кастомная майндмэп-структура семантики (`.st-*`) — см. раздел 6.1
/js/main.js                 — общая логика интерфейса: FAQ-аккордеон, лайтбокс сертификатов (`abOpenLightbox`/`abOpenLightboxSrc`), попапы (открыть/закрыть), мобильное меню-гамбургер (открыть/закрыть), закрытие по Esc и клику вне, слайдеры на мобильном (`abReviewsSlide`/`abProcessSlide`/`abSolutionSlide`), раскрывашки текста (`abAuditIntroToggle`, `abAuditCardToggle`), бесконечная галерея креативов на кейс-странице (`abCaseGallerySlide`, см. раздел 6.1)
/js/forms.js                 — отправка всех форм на Web3Forms (см. раздел 4)
/js/cookie-consent.js        — баннер согласия на cookie + отложенная загрузка аналитики (см. раздел 8)
/js/semantic-tree.js         — самописный (без внешних библиотек) генератор интерактивной майндмэп-схемы семантики для кейс-страницы, см. раздел 6.1

/images/                    — все картинки сайта (фото, сертификаты, иконки, favicon)
/images/cases/bani-bochki/  — фото и скриншоты для кейса «Бани-бочки» (см. раздел 6.1)
```

### Что лежит в /images/

| Файл | Для чего |
|---|---|
| `my-photo.jpg` | фото в хиро-блоке главной |
| `about-photo.jpg` | фото в блоке «Обо мне» |
| `certificate-direct.png`, `certificate-metrika.png` | круглые бейджи сертификатов в хиро-блоке |
| `direct2026.jpg`, `metrika2026.jpg` | полные сканы сертификатов (блок «Подтверждённая экспертиза», открываются в лайтбоксе по клику) |
| `review-telegram-1.png`, `review-telegram-2.png`, `review-telegram-3.png` | реальные скрины переписки-отзывов в Telegram (Алевтина «Салон Точка», Виталий «Юрист Арбитраж», Роман), блок «Что говорят клиенты» |
| `face-avatar.jpg` | круглое фото лица — используется в мобильной шапке рядом с именем |
| `favicon.png` | иконка вкладки браузера, подключена на всех страницах сайта |

## 3. Все формы на сайте

На сайте сейчас **6 живых форм**, все ведут на `thanks.html`.

| Название (внутреннее) | Где на сайте | Поля (name → что значит) |
|---|---|---|
| **Форма внизу главной** (`id="form-request"`, секция `#request`) | Финальный тёмный блок в самом низу главной страницы | `name` — имя клиента **(обязательно)**; `messenger` — поле подписано «Контакт для связи», плейсхолдер «Мессенджер или телефон» **(обязательно)**; `link` — ссылка на сайт/рекламный аккаунт (необязательно); `comment` — комментарий (необязательно) |
| **forma-audit** (`id="popup-audit"`) | Попап «Получите бесплатный разбор рекламы» (бесплатный аудит). Открывается с кнопок: «Получить аудит» (шапка), «Получить бесплатный аудит» (хиро), «Получить бесплатный аудит» (карточка боли №2), «Получить бесплатный аудит» (блок «Бесплатный разбор аккаунта»), «Узнать цену заявки в моей нише» (блок «Пример фрагмента аудита») | `name` — имя (необязательно); `phone` — телефон, плейсхолдер `+7 (000) 000-00-00` **(обязательно, проверяется формат — см. раздел 4)** |
| **forma-format** (`id="popup-format"`) | Попап «Обсудить формат» — общий разговор про формат сотрудничества (не привязан к конкретному тарифу). Открывается с кнопок: «Обсудить формат» (карточка боли №3), «Обсудить формат работы» (карточки сравнения «Три способа получить специалиста») | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)** |
| **forma-obsudit** (`id="popup-obsudit"`) | Попап «Запуск и ведение Директа» — про конкретный тариф. Открывается только с кнопки «Обсудить запуск и ведение» (тариф «Запуск и ведение Яндекс Директа») | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)**; `link` — ссылка на сайт или кабинет (необязательно) |
| **forma-audit-paid** (`id="popup-audit-paid"`) | Попап «Заказать платный аудит» — про тариф «Аудит». Открывается только с кнопки «Заказать аудит» (тариф «Аудит», блок «Услуги и цены») | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)**; `link` — ссылка на сайт или кабинет (необязательно) |
| **forma-mediaplan** (`id="popup-mediaplan"`) | Попап «Получить медиаплан». Открывается с кнопки «Получить бесплатный медиаплан» (карточка боли №1) | `name` — имя (необязательно); `phone` — телефон **(обязательно, проверяется формат)** |

**Важно:** до 2026-08-28 кнопки «Обсудить формат», «Обсудить формат работы», «Обсудить запуск и ведение» и «Заказать аудит» все вели на один и тот же попап `popup-obsudit` («Обсудить проект», общий текст). Его разделили на 3 отдельных попапа с разным текстом под каждую кнопку (см. таблицу выше) — старое название `popup-obsudit`/`form-obsudit` сохранено только за тарифом «Запуск и ведение», две другие кнопки получили новые попапы `popup-format` и `popup-audit-paid`. Всё это работает поверх одного и того же `js/forms.js` без единой правки в JS — скрипт находит любую форму по атрибуту `data-redirect` и собирает её поля автоматически, так что добавление нового попапа = просто скопировать разметку с новым `id`/`subject`.

Во всех живых попапах-заявках (`forma-audit`, `forma-format`, `forma-obsudit`, `forma-audit-paid`, `forma-mediaplan`) над полем телефона стоит ряд из 3 кнопок-ссылок «Написать в мессенджер» (Telegram / MAX / WhatsApp, ведут на `t.me/a_beliakov`, `max.ru/...`, `wa.me/79890473181`) — это не часть формы, просто быстрый способ написать напрямую, минуя отправку заявки.

Общие для всех форм технические поля:
- `consent` — чекбокс согласия на обработку персональных данных. **Обязателен**: без галочки форма не отправится, рядом появится подсказка.
- Поля, помеченные выше как «обязательно», тоже не дают отправить форму пустыми — браузерная стандартная подсказка отключена (`novalidate`), вместо неё показывается своя подсказка на русском рядом с кнопкой и подсвечивается конкретное незаполненное поле.
- `subject` (скрытое поле) — пишет в письмо, какая именно форма/страница прислала заявку, например «Заявка с сайта — попап «Заказать платный аудит»». Без этого поля все письма выглядели бы одинаково.
- `botcheck` (скрытое поле-ловушка для ботов, honeypot) — см. раздел 4.

## 4. Как устроена отправка форм

- **Сервис:** [Web3Forms](https://web3forms.com) — сторонний сервис приёма форм без бэкенда, письмо с содержимым формы приходит на почту владельца ключа. Общий для всех форм access key хранится прямо в `js/forms.js` (не в этом файле — ключ намеренно не публикуется в документации).
- **Как работает `js/forms.js`:** один общий скрипт находит на странице все `<form data-redirect="...">`, при отправке:
  1. Проверяет обязательные текстовые поля (`required`, но с отключённой браузерной подсказкой через `novalidate`). Если что-то пустое — форма не отправляется, показывается своя подсказка на русском, и фокус ставится на первое незаполненное поле. Поле `email` дополнительно проверяется на формат (должно быть похоже на настоящий email), поле `phone` — на формат телефона (только цифры/пробелы/`+`/скобки/дефисы, и 10–11 цифр внутри), иначе — своя подсказка «Введите настоящий email/номер телефона, например ...».
  2. Проверяет чекбокс `consent`. Если не отмечен — форма не отправляется, показывается подсказка рядом с галочкой.
  3. Собирает все реально существующие поля формы через `FormData` и отправляет POST-запросом на `https://api.web3forms.com/submit`.
  4. Если Web3Forms ответил успехом — в `window.dataLayer` пушится событие `{ event: 'form_sent', form_id: '<id формы>' }` (на случай, если на сайт когда-нибудь подключат Google Tag Manager), событие дублируется в консоль браузера (`console.log('form_sent', ...)` — видно в devtools при тестировании), и сразу вызывается цель Яндекс.Метрики `ym(111606163, 'reachGoal', 'form_sent')` (если счётчик уже загружен — см. раздел 8, счётчик грузится только после согласия на cookie). После этого браузер переходит на адрес из `data-redirect` (сейчас у всех живых форм это `thanks.html`).
  5. Если сеть недоступна или Web3Forms вернул ошибку — кнопка возвращается в исходное состояние, рядом с формой показывается текст «Не получилось отправить, напишите мне напрямую в Telegram» со ссылкой на https://t.me/a_beliakov.
- **Поле `subject`** — задаёт «тему» письма на стороне Web3Forms, по нему в почте сразу видно, с какой именно кнопки/попапа пришла заявка.
- **Honeypot (`botcheck`)** — скрытый чекбокс, невидимый обычному человеку (`display:none`), но который боты-скрипты иногда всё равно заполняют. Web3Forms распознаёт такие отправки как спам и отсекает их. Реализовано на всех формах сайта.
- **Куда приходит почта:** на адрес, привязанный к access key в личном кабинете Web3Forms (настраивается на их стороне, в коде сайта этого адреса нет).
- **Событие `form_sent`** — при каждой успешной отправке любой из живых форм: пушится в `window.dataLayer` с полем `form_id` (на будущее, если подключат GTM), логируется в консоль браузера и сразу засчитывается как достижение цели `form_sent` в Яндекс.Метрике через `ym(111606163, 'reachGoal', 'form_sent')`. По полю `form_id` можно различать, какая именно форма сработала (это видно в dataLayer/консоли, у самой цели Метрики form_id не передаётся).

## 5. Якоря и ссылки

| Якорь/ссылка | Где ведёт | Комментарий |
|---|---|---|
| `#services` | Секция «Услуги и цены» на главной | пункт «Цены» — в меню шапки на ПК и в мобильном выпадающем меню |
| `#cases` | Секция «Результаты в цифрах» на главной | пункт «Кейсы» — в меню шапки на ПК и в мобильном выпадающем меню |
| `#about` | Секция «Кто будет вести вашу рекламу» (Обо мне) на главной | пункт «Обо мне» — в меню шапки на ПК и в мобильном выпадающем меню |
| `#reviews` | Секция «Что говорят клиенты» на главной | пункт «Отзывы» — только в мобильном выпадающем меню (на ПК этого пункта в шапке нет) |
| `#faq` | Секция «Частые вопросы» на главной | пункт «Вопросы» — только в мобильном выпадающем меню (на ПК этого пункта в шапке нет) и в навигации футера |
| `#free-audit`, `#audit-example` | Блок бесплатного аудита / блок-пример аудита | пункт «Бесплатный разбор» — только в мобильном выпадающем меню; также кнопка «Посмотреть пример аудита →» ведёт на `#audit-example` |
| `#request` | Секция с финальной формой внизу главной (она же футер) | используется как fallback-адрес у кнопок-триггеров попапов (если JS не сработает — просто проскроллит к форме внизу) |
| `privacy.html` | Страница политики конфиденциальности | ссылка в чекбоксах согласия всех форм |

**Важно (с 2026-09-09):** меню шапки на ПК и мобильное выпадающее меню — теперь два разных набора пунктов, а не одно и то же меню, которое просто по-разному показывается. На ПК в `<nav class="ab-header-nav">` только 3 пункта (Цены/Кейсы/Обо мне). На мобильном по нажатию на гамбургер открывается отдельный `<nav class="ab-header-nav-mobile">` с 6 пунктами (Цены/Кейсы/Обо мне/Отзывы/Вопросы/Бесплатный разбор). Открывает/закрывает его та же функция `abToggleMobileNav` в `js/main.js` — она ищет `.ab-header-nav-mobile`, а на `pilot.html` (где этой отдельной мобильной nav нет) с откатом ищет старый `.ab-header-nav`, чтобы не сломать архивную страницу. **Если понадобится добавить/убрать пункт меню** — правьте оба списка отдельно, так как теперь они не связаны.

## 6. Что сделано и что отложено

**Структура главной страницы (`index.html` / `new.html`), сверху вниз:**
1. **Шапка** — логотип, меню на ПК (Цены/Кейсы/Обо мне), город, телефон, мессенджеры, кнопка «Получить аудит». На мобильном — компактная плоская полоса с фото-аватаром, контакт-тогглом и гамбургером; гамбургер открывает отдельное выпадающее меню из 6 пунктов (Цены/Кейсы/Обо мне/Отзывы/Вопросы/Бесплатный разбор) — подробности и полный список якорей см. раздел 5.
2. **Hero** (кремовый фон) — капсула «Частная практика · 10 лет в Директе» (`align-self:flex-start`, чтобы не растягивалась на всю ширину колонки — без этого свойства она растягивалась из-за flex-родителя, баг починен 2026-08-28), H1 «Ваш эксперт по Яндекс Директу / Без стажеров и посредников» (без точек), короткий подзаголовок про экономику и цену заявки, один CTA «Получить бесплатный аудит», строка из 3 кратких цифр (10 лет — в контекстной рекламе / 1 человек — от брифа до отчета / 0% — от бюджета, только фикс), фото специалиста и 2 карточки сертификатов (Яндекс.Директ / Яндекс.Метрика — открываются в лайтбоксе по клику «Посмотреть сертификат», НЕ ведут на yandex.ru). Левая колонка (текст) верстается простым `flex-direction:column` без растягивания на высоту правой колонки (фото) — раньше была связка `height:100%;justify-content:space-between`, из-за которой при сокращении текста между CTA и строкой цифр появлялся некрасивый пустой отступ; убрано 2026-09-08.
3. **Плашка под hero** (белый фон, 3 колонки) — «Аккаунт ваш» / «Договор» / «2 часа», с короткой подписью под каждой.
4. **«Скорее всего, вы в одной из этих ситуаций»** (кремовый фон) — 3 карточки боли, у каждой свой CTA и попап (медиаплан / бесплатный аудит / «Обсудить формат», см. раздел 3), под карточками закрывающая фраза.
5. **«Три способа получить специалиста по Директу»** (сравнение, белый фон) — карусель из 10 карточек-критериев («Кто на самом деле ведёт рекламу», «Сколько это стоит» и т.д.), в каждой карточке сравниваются 3 варианта: «Частный специалист» (я, выделен) / «Сотрудник в штате» / «Агентство». На мобильном — свайп с точками-индикаторами (классы `.ab-solution-slider`/`.ab-solution-row`/`.ab-solution-card`, функция `abSolutionSlide`, высота строк карточек синхронизируется JS-функцией `abSyncSolutionRowHeights`).
6. **Раскрывающаяся карточка «Сколько на самом деле стоит «свой директолог в штате»»** (кремовый фон) — статичная, не сворачивается (класс `ab-audit-card-static`), таблица расходов: зарплата 100–150 тыс ₽, налоги/взносы 50–75 тыс ₽, отпуск и прочее 10–15 тыс ₽, время владельца 5–10 часов, итого 160–240 тыс ₽/мес + время.
7. **«Отвечаю за заявку, а не за клик»** (Зона ответственности, белый фон) — 4 карточки («Сайт и посадочная», «Метрика и аналитика», «Обработка заявок», «Экономика проекта»), у каждой кремовый фон, серая рамка, компактная оранжевая иконка-квадрат сверху (переоформлено 2026-08-27 под единый стиль остальных карточных блоков сайта).
8. **`id="free-audit"` «Бесплатный разбор вашего рекламного аккаунта»** (кремовый фон) — на мобильном (≤760px) весь текст-интро спрятан за маленькой кнопкой «?» рядом с заголовком (`abAuditIntroToggle`), на ПК текст виден всегда. Справа карточка «Что внутри» с 5 пунктами, CTA «Получить бесплатный аудит».
9. **`id="audit-example"` «Пример фрагмента аудита»** (белый фон) — карточки-примеры находок аудита (высота строк внутри карточек выровнена по CSS-flex), CTA «Узнать цену заявки в моей нише» (открывает форму аудита).
10. **`id="cases"` «Результаты в цифрах»** (кремовый фон) — 3 карточки примеров («Окна ПВХ», «Доставка цветов», «Дома из бруса») без ссылок «Подробнее →» (убраны 2026-09-14 вместе с удалением страниц-заглушек, на которые они вели). Карточки не связаны с реальным кейсом `/cases/bani-bochki-mo.html` (см. раздел 6.1) — он существует отдельно и пока не встроен в этот блок.
11. **`id="process"` «Как проходит работа»** (белый фон) — 6 карточек-шагов (Заявка и брифинг → Аудит/анализ ниши → Стратегия → Настройка и запуск → Чистка и оптимизация → Масштабирование), на мобильном превращены в слайдер по одной карточке со свайпом и точками-индикаторами (классы `.ab-process-slider`/`.ab-process-row`, функция `abProcessSlide`). На ПК — сетка 3×2.
12. **«Кто будет вести вашу рекламу»** (Обо мне, белый фон) — фото + 3 абзаца текста про подход и опыт. На мобильном (≤760px) текст спрятан за кнопкой «?» рядом с заголовком (тот же механизм `abAuditIntroToggle`/`.ab-audit-intro-text`, что и в блоке «Бесплатный разбор аккаунта», см. пункт 8 — переиспользован без изменений в JS/CSS).
13. **`id="services"` «Услуги и цены»** (кремовый фон) — **2 тарифа**: «Запуск и ведение Яндекс Директа» — **35 000 ₽/мес** (кнопка «Обсудить запуск и ведение» → попап `popup-obsudit`, см. раздел 3), «Аудит» — **20 000 ₽ разово, срок 3-5 рабочих дней** (кнопка «Заказать аудит» → отдельный попап `popup-audit-paid`, «Заказать платный аудит»). Карточка «Аудит» честно отстроена от бесплатного разбора («Хотите независимую оценку. Бесплатный разбор показывает 3-5 главных проблем — здесь разбираю аккаунт целиком и отдаю план правок»), 4 пункта, что входит, отдельным блоком — что **не входит** («внесение правок в аккаунт»). Внизу ссылка «Или начните с бесплатного разбора» на `#free-audit`.
14. **`id="faq"` «Частые вопросы»** (кремовый фон, аккордеон `abFaqToggle`) — 10 вопросов: «Боюсь дать доступ к аккаунту...», «Почему не взять директолога в штат?», «У меня уже настроены кампании. Их придётся переделывать?», «Нам уже делали Директ, не работал. Почему у вас будет иначе?», «Как понять, что вы не исчезнете через месяц?», «Сколько нужно бюджета, чтобы пошли заявки?», «Как я пойму, что реклама работает?», «Вы работаете удалённо?», «Как происходит оплата?», «Чем вы отличаетесь от других частных специалистов?».
15. **«Что говорят клиенты»** (белый фон) — 3 реальных скрина переписки в Telegram (Алевтина, Виталий, Роман — см. таблицу картинок в разделе 2), статичные фото без лайтбокса. На мобильном — слайдер со свайпом.
16. **«Подтверждённая экспертиза»** (кремовый фон) — полные сканы сертификатов, кнопки «Проверить сертификат» ведут на yandex.ru в новой вкладке.
17. **`id="request"`** (тёмный фон) — финальная форма заявки + футер в одном блоке: 3 колонки (имя/подпись/«на связи»/мессенджеры/почта → навигация → форма), под ними реквизиты («Самозанятый Беляков А.В., ИНН 501811176988. Работаю по договору», ссылка на политику, год). Отдельного `<footer>` на сайте нет.

**Прочее сделано и работает:**
- Единая шапка на всех страницах сайта; на мобильном (≤600px) — компактная плоская полоса с фото-аватаром, контакт-тогглом и гамбургером (гамбургер на `index.html`/`new.html` открывает 6 пунктов, см. раздел 5; на `pilot.html` — старое меню из 3 пунктов), оба меню закрываются по клику вне/по Esc/по пункту меню. Контакт-тоггл открывает список «Позвонить» + 3 мессенджера, у каждого пункта своя иконка слева (зелёный телефон, брендовые Telegram/MAX/WhatsApp — те же SVG, что и в кнопках попапов). На главной под шапкой на мобильном — растянутая капсула «Сертифицированный специалист», сама шапка зафиксирована при прокрутке (`position: fixed`).
- 6 живых форм подключены к Web3Forms (см. разделы 3–4): обязательные поля и согласие валидируются на русском без системных браузерных подсказок, honeypot от ботов, единая страница благодарности `thanks.html`, событие `form_sent` при успехе — в dataLayer, в консоль браузера и как достижение цели в Яндекс.Метрике.
- Favicon подключён на всех страницах через `<link rel="icon">` (`images/favicon.png`); дополнительно с 2026-09-13 в корне сайта лежит `/favicon.ico` (см. раздел 2) — на случай, если браузер или внешний сервис запрашивает иконку по стандартному адресу напрямую, не читая `<head>`.
- Иконки мессенджеров в шапке — брендовые SVG (Telegram/WhatsApp/MAX), одинаковые круглые кнопки 40×40px с hover-анимацией.
- Все оранжевые кнопки на сайте плавно темнеют при наведении (`#FF5A1F` → `#E8480F`).
- Ссылка на Telegram везде на сайте — `https://t.me/a_beliakov`.
- Баннер согласия на cookie на всех живых страницах — Яндекс.Метрика загружается только после согласия пользователя (см. раздел 8).
- Email на сайте (футер главной, `privacy.html`) — `me@alex-belyakov.com`.
- Раздел «Бесплатные материалы» (лид-магниты/гайды) **полностью удалён с сайта** (был архивирован, затем убран начисто вместе со всеми упоминаниями в коде). Текстовая копия материалов, картинки и инструкция для разработчика по восстановлению сохранены во внешнем архиве — `D:\My Docs\My Agency\alex-belyakov.com\Мой Сайт по Контексту\Бесплатные материалы на сайте\Архив с сайта` (папка вне репозитория).

**Осознанно отложено:**
- **Кейс «Бани-бочки» не связан ссылками с главной страницей.** Страница `/cases/bani-bochki-mo.html` полностью готова (см. раздел 6.1), но карточки в блоке «Результаты в цифрах» на неё пока не ссылаются — там остались 3 анонимных примера без ссылок (см. пункт 10 выше). Страница открыта только по прямому адресу и намеренно скрыта от поисковиков (`noindex`, не в `sitemap.xml`, см. раздел 7) — пока не решено, встраивать ли её в блок примеров или оставить отдельным лендингом под рекламу/рассылку. **Когда будет решение:** либо добавить ссылку с главной + снять `noindex` + добавить в `sitemap.xml`, либо оставить как есть для точечных ссылок.

### 6.1. Кейс-страницы (`/cases/`)

Единственный кейс на сегодня — **«Бани-бочки»** (`cases/bani-bochki-mo.html`), Москва и область, целевые обращения выросли с 4 до 54 в месяц, цена целевого обращения упала почти в 4 раза. История страницы: изначально было две параллельные версии для сравнения вёрстки — `bani-bochki-mo.html` (первый вариант) и `bani-bochki-mo-1.html` (переработанный по референсам заказчика). 2026-09-22 выбранная версия (бывшая `-1`) перенесена в `bani-bochki-mo.html`, вторая страница удалена — сравнение больше не нужно, кейс существует в одном экземпляре.

Структура страницы сверху вниз: та же шапка, что и на главной (см. раздел 5), hero с бейджем «Кейс · Яндекс Директ», заголовком, 4 полями meta (Ниша/Регион/Канал/Срок), фото первого экрана сайта клиента и блоком из 2 KPI (было→стало) — далее блок «С чем пришёл клиент», блок «Что сделал» (7 пронумерованных пунктов простым текстом, без карточек), таблица «Результаты» (на мобильном превращается в стек карточек через `data-label` + media-запрос в `.ab-case-table`), блок «Инсайты» (простой список из 3 пунктов, до 2026-09 назывался «Что не сработало»), финальный тёмный CTA-блок с фото и подписью «Александр Беляков · эксперт по Яндекс Директу» и попапом заявки (`popup-audit`/`form-audit` — та же разметка, что и в остальных попапах сайта, см. раздел 3, JS-правок не потребовалось).

Два самописных интерактивных элемента внутри пункта «Что сделал»:
- **Интерактивная структура семантики** (`js/semantic-tree.js`) — небольшое превью-дерево (`#st-preview`) внутри статьи, по клику открывает модальное окно (`#map-modal`) с полноценным майндмэпом: 9 групп, ~79 ключевых фраз, пан мышью/тачем и зум (колесо+Ctrl, кнопки +/−/«показать целиком»). Реализовано полностью на ванильном JS через DOM + CSS `transform`, без внешних библиотек (до 2026-09 использовалась библиотека markmap — убрана целиком по просьбе заказчика: «убери и перепиши чистым кодом»).
- **Галерея креативов РСЯ** (`.ab-case-gallery` в CSS, `abCaseGallerySlide` в `js/main.js`) — 6 реальных скриншотов объявлений (`images/cases/bani-bochki/rsya-1.jpg`…`rsya-6.jpg`), листается стрелками по одной карточке (3 видно на ПК, 1 на мобильном). С 2026-09-22 прокрутка бесконечная по кругу (список слайдов клонируется в JS по обе стороны, при подходе к краю позиция бесшовно переставляется на соответствующее место в соседнем клоне) — нет ни упора в край, ни видимой полоски прокрутки под галереей (`scrollbar-width:none` + `::-webkit-scrollbar{display:none}` в CSS).

Плюс одиночный скриншот объявления в поиске (`images/cases/bani-bochki/search-ad.jpg`) после пункта «Чистка поиска».

**При правке CSS/JS, которые затрагивают кейс-страницу** — не забывать бампать `?v=N` у `<link rel="stylesheet" href="../css/style.css?v=N">` в `cases/bani-bochki-mo.html`, иначе браузер/CDN может отдать старую закэшированную версию стилей (так уже было один раз, страница выглядела визуально сломанной на боевом сайте, пока не подняли версию).

## 7. Индексация поисковиками

Сайт зарегистрирован в Яндекс.Вебмастере 2026-08-27 (файл-подтверждение `yandex_bdb97dc3a28e4f23.html`, см. раздел 2 — не удалять). Индексация открыта не полностью:

- **`robots.txt`** — разрешает роботам всё, кроме `/new.html` и `/pilot.html` (рабочий черновик и архив старой версии — не должны попадать в поиск). Дополнительно (с 2026-09-13) содержит явные `Allow: /` блоки для real-time ботов ИИ-поисковиков, которые заходят на сайт «вживую» в момент, когда пользователь задаёт вопрос в ChatGPT/Claude/Perplexity: `OAI-SearchBot`, `ChatGPT-User`, `Claude-SearchBot`, `Claude-User`, `PerplexityBot`. Файл заканчивается строкой `Sitemap:` со ссылкой на карту сайта.
  - **Важно:** блокировка ботов-краулеров, которые собирают данные для обучения ИИ (`GPTBot`, `ClaudeBot`, `Google-Extended` и т.п.), в этом файле **намеренно отсутствует** — она настроена на уровне Cloudflare и автоматически подмешивается в реально отдаваемый `robots.txt` как «Cloudflare Managed content». В репозитории её быть не должно — не дублировать и не пытаться «дописать недостающее», если вдруг покажется, что чего-то не хватает.
- **`sitemap.xml`** — список реальных страниц, которые стоит индексировать. Сейчас там только `/` (главная) и `/privacy.html`. Технические страницы (`thanks.html`, `404.html`) в карту сайта намеренно не включены. Кейс `cases/bani-bochki-mo.html` тоже пока не включён (см. раздел 6.1) — **при появлении новых индексируемых страниц сайта карту нужно дополнять вручную**, сама она не обновляется.
- **Мета-тег `<meta name="robots" content="noindex, nofollow">`** оставлен точечно на 3 страницах, которые не должны попадать в поиск: `new.html`, `pilot.html`, `cases/bani-bochki-mo.html` (см. раздел 6.1, почему кейс пока скрыт). На всех остальных живых страницах (`index.html`, `thanks.html`, `404.html`, `privacy.html`) тега нет — они открыты для индексации.
- **`<link rel="canonical" href="https://direct.alex-belyakov.com/">`** — стоит в `<head>` только `index.html` (добавлен 2026-09-14), чтобы поисковики считали канонической версию без `/index.html` на конце адреса. На `new.html` не нужен — она и так `noindex`.
- Все живые страницы уже имеют уникальные `<title>`, `<meta description>` и ровно один `<h1>` — этим отдельно заниматься не нужно, проверено 2026-08-27.
- **Open Graph теги** (`og:type`, `og:locale`, `og:url`, `og:title`, `og:description`, `og:image`) добавлены в `<head>` `index.html`/`new.html` 2026-09-13 — для корректных превью при расшаривании ссылки на сайт в соцсетях/мессенджерах. `og:image` указывает на `images/my-photo.jpg` (фото портретное, не альбомное 1200×630 — соцсети могут слегка обрезать превью по краям, но само превью работает).

## 8. Внешние сервисы

- **Web3Forms** — приём и пересылка данных всех форм сайта на почту владельца. Никакого бэкенда/базы данных на сайте нет, вся обработка форм происходит на стороне Web3Forms. Access key используется единый для всех форм, хранится в `js/forms.js` (сам ключ в этой документации не публикуется).
- **GitHub Pages** — хостинг статического сайта. Домен `direct.alex-belyakov.com` подключён через файл `CNAME` в корне репозитория + настройку custom domain в настройках репозитория на GitHub (там же выпускается HTTPS-сертификат).
- **Google Fonts** — подключение шрифта Golos Text через `<link>` на каждой странице (CDN Google, не требует настройки).
- **Яндекс.Вебмастер** — сайт зарегистрирован и подтверждён HTML-файлом (см. раздел 7). Отдельного счётчика/скрипта Вебмастера в коде нет, подтверждение владения — разовое, через статичный файл.
- **Яндекс.Метрика + баннер согласия на cookie** — счётчик (id `111606163`) больше **не** вставлен напрямую в `<head>`. Вместо этого на каждой странице перед `</body>` подключён `<script src="/js/cookie-consent.js" defer></script>` (путь абсолютный от корня — работает одинаково на страницах любой вложенности).
  - При первом визите показывается компактная тёмная карточка (не на всю ширину, плавает снизу слева) с текстом, ссылкой на `/privacy.html#cookies` и одной кнопкой «Принять». Кнопки «Отклонить» нет — согласие даётся явным кликом или продолжением пользования сайтом.
  - Согласие сохраняется в `localStorage.cookie_consent = 'accepted'`. Пока пользователь не нажал «Принять», карточка показывается на каждой странице.
  - Код счётчика Метрики (те же id и параметры, что были раньше) теперь живёт внутри функции `loadAnalytics()` в `js/cookie-consent.js` и вызывается только при нажатии «Принять» или если согласие уже было дано ранее. При отказе или до выбора — счётчик не грузится вообще.
  - Google Ads/Analytics на сайте пока не подключены; когда появится код счётчика — его нужно добавлять точно так же, внутрь `loadAnalytics()`, а не отдельной вставкой в `<head>`.
  - **При создании любой новой страницы сайта** счётчик в `<head>` добавлять не нужно — достаточно подключить `js/cookie-consent.js` перед `</body>`, как на остальных живых страницах.
- **JivoChat (виджет чата)** — подключён `<script src="//code.jivo.ru/widget/ufWAawj4Vq" async></script>` в самом конце `<body>`, после `cookie-consent.js`, на всех 7 страницах сайта (`index.html`, `pilot.html`, `new.html`, `404.html`, `thanks.html`, `privacy.html`, `cases/bani-bochki-mo.html`). Подключается напрямую с `code.jivo.ru`, без баннера согласия (в отличие от Метрики) — так решил заказчик. До этого перебирали Tawk.to и Crisp — оба убраны полностью (см. историю коммитов), сейчас на сайте только JivoChat. **При создании любой новой страницы** этот блок тоже нужно добавлять перед `</body>`.
